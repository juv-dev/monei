name: Workflow PR Unit Tests

on:
  pull_request:
    branches: [main]

jobs:
  Pull_Request:
    name: PR
    uses: juv-dev/workflows-frontend/.github/workflows/frontend-pr-unit-test.yml@main
    secrets: inherit
